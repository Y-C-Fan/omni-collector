{
  "name": "fav-collector-local",
  "version": "0.8.9",
  "description": "Fav Collector (local-only): one-click favorites sync for Bilibili/YouTube/Zhihu/X, no engine, no database.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node esbuild.config.mjs production",
    "test": "vitest run",
    "dev": "node esbuild.config.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "obsidian": "^1.5.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}